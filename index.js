console.log("Hello Project Management App!");

if (process.env.NODE_ENV !== "production") {
  require("dotenv").config();
  if (process.env.NODE_ENV !== "production") {
    require("dotenv").config();
  }
}
